name: Unity Sprint Pipeline

on:
  push:
    branches:
      - 'sprint-*'
  pull_request:
    branches:
      - 'sprint-*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache Unity License
        uses: actions/cache@v4
        with:
          path: ~/.local/share/unity3d
          key: ${{ runner.os }}-unity-license-${{ hashFiles('**/ProjectSettings/ProjectVersion.txt') }}
          restore-keys: |
            ${{ runner.os }}-unity-license-

      - name: Create TestResults directory
        run: mkdir -p TestResults

      - name: Run Unity PlayMode Tests (no Docker)
        run: |
          unity-editor \
            -batchmode \
            -nographics \
            -projectPath . \
            -runTests \
            -testPlatform PlayMode \
            -testResults TestResults/PlayModeResults.xml \
            -logFile -
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: PlayModeResults
          path: TestResults/PlayModeResults.xml
